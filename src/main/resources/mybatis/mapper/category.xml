<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.toris93.nanjung.web.dao.CategoryMapper"> 

	<select id="getCategory" resultType="com.toris93.nanjung.web.domain.CategoryVO"> 
		SELECT
			category_id, name
		FROM
			category
		
	 </select> 
	

 </mapper>